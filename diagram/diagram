Table users {
  id uuid [pk]
  name varchar
  email varchar [unique]
  password varchar
  type varchar // CUSTOMER, BUSINESS, ADMIN
  address_id uuid [ref: > addresses.id]
  createdAt timestamp [default: `now()`]
  updatedAt timestamp [default: `now()`]
}

Table addresses {
  id uuid [pk]
  street varchar
  number varchar
  city varchar
  state varchar
  zipcode varchar
  type varchar
  complement varchar
  createdAt timestamp [default: `now()`]
  updatedAt timestamp [default: `now()`]
}

Table wallets {
  id uuid [pk]
  user_id uuid [ref: > users.id]
  amount decimal(10,2)
  currency varchar [default: `'BRL'`]
  createdAt timestamp [default: `now()`]
  updatedAt timestamp [default: `now()`]
}

Table transactions {
  id uuid [pk]
  wallet_id uuid [ref: > wallets.id]
  address_id uuid [ref: > users.address_id]
  type varchar // money, wallet
  amount decimal(10,2)
  description text
  createdAt timestamp [default: `now()`]
  updatedAt timestamp [default: `now()`]
}
